const Modelo = require("./classes/Manager");

const manager = new Modelo();

// manager.createEvent({id:'Clasica2',title:'Clasica roja',talle:'22',price:900,
// size:'small'}).then(result=>{
//     console.log(result.message);
// })

// manager.getById("Clasica2")

// manager.getAll()

// manager.deleteAll()

// manager.deleteById()

// manager.getProductRandom()